-- Quick diagnostic SQL for InfinityFree
-- Run this first to see your table structure

-- Check if users table exists and show its structure
DESCRIBE users;

-- Show all tables in database
SHOW TABLES;

-- If users table exists, show sample data
SELECT * FROM users LIMIT 5;
