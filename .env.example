# Copy this file to .env and fill in your production credentials.
# Do NOT commit .env to version control. The .gitignore already ignores it.

# MySQL / MariaDB credentials
DB_HOST=sql312.infinityfree.com
DB_NAME=if0_40000657_gadighar
DB_USER=if0_40000657
DB_PASS=REPLACE_WITH_DB_PASSWORD
